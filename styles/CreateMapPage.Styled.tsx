import styled from 'styled-components'

const StyledCreateMapPage = styled.div`
  // Override max-width in Layout.Styled
  .mainContent {
    max-width: 2000px;
  }
`

export default StyledCreateMapPage
