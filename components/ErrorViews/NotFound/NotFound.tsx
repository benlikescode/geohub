import React, { FC } from 'react'
import { StyledNotFound } from '.'

type Props = {
  
}

const NotFound: FC<Props> = ({}) => {
  return (
    <StyledNotFound>
      
    </StyledNotFound>
  )
}

export default NotFound