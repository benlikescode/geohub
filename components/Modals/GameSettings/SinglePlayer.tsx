import { UserIcon, UserGroupIcon, ClockIcon, ArrowsExpandIcon, SwitchHorizontalIcon, ZoomInIcon } from "@heroicons/react/outline"
import React from "react"
import { formatTimeLimit } from "../../../utils/helperFunctions"
import { FlexGroup, Avatar, Checkbox, Icon, Slider } from "../../System"


const SinglePlayer = () => {
  return (
    <>
    
    </>
  )
}

export default SinglePlayer